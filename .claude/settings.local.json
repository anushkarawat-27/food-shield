{
  "permissions": {
    "allow": [
      "Bash(mkdir -p ingestion/sources db/migrations simulator optimizer api/routes api/services agent web infra)",
      "Bash(git init *)",
      "Bash(python3 -c \"import xgboost, joblib, sklearn, numpy, pandas; print\\('deps OK', xgboost.__version__\\)\")",
      "Bash(pip install *)"
    ]
  }
}
